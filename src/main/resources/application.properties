spring.datasource.url=jdbc:postgresql://localhost:5432/contacts2
spring.datasource.username=postgres
spring.datasource.password=Fzy84C
spring.datasource.driver-class-name=org.postgresql.Driver

#Jetty
#Note  Jetty is used because When using Apache TomCat, an error occurs when sending a regular expression to RESTful controller
server.port=8081
#server.servlet.context-path=/my
#server.compression.enabled: true
#server.compression.min-response-size: 1
#server.jetty.acceptors=2

#Cache Server
# See also: http://docs.spring.io/spring-boot/docs/current/reference/html/boot-features-hazelcast.html
#By default :  Spring Boot tries to find the Hazelcast configuration
# from the default locations, that is hazelcast.xml in the working directory
# or at the root of the classpath.
# Alternative location:  spring.hazelcast.config=classpath:my-hazelcast.xml

#server
spring.cache.type=hazelcast
#spring.main.web-environment=false
